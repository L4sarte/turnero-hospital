// Script principal del Turnero
// Se inyecta desde el HTML para mantener el codigo JS separado

// Año actual en el footer - se actualiza solo cada año
document.getElementById('current-year').textContent = new Date().getFullYear();
